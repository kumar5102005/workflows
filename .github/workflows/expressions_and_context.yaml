name: Expressions and Contexts
on: [push, pull_request]
run-name: expressions and context by @${{ github.actor}} ${{ github.event_name}}

jobs:
  Expressions:
    runs-on: ubuntu-latest
    steps:
    - name: Expressions
      run: |
        echo ${{ 1 }}
        echo ${{ 1>2 }}
        echo ${{ 4 || 5}}
    
    - name: Contexts
      run: |
        echo '${{ toJson(github) }}'